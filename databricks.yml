# This is a Databricks asset bundle definition for lakeflow_pipelines_python.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: de_enhancements
  uuid: 87a174ba-60e4-4867-a140-1936bc9b00de

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  notifications:
    description: The email addresses to use for failure notifications
  user_name:
    description: user_name used in prod

targets:
  dev:

    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: production
    default: true
    workspace:
      host: https://dbc-c741477f-0f7d.cloud.databricks.com/?o=3178695042235081
    variables:
      user_name: dde9580d-c31d-4acf-8481-2cacab11e873
      catalog: catalog
      schema: ${workspace.current_user.short_name}
      notifications: []
    resources:
      dashboards:
        bakehouse_summary: 
          file: ./Workspace/dashboard/Bakehouse_Summary.lvdash.json
          warehouse_id: 3178695042235081
          display_name: bakehouse_summary
          name: bakehouse_summary


