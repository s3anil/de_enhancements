resources:
  dashboards:
    my_dashboard:
      path: /Volumes/sales_review/sales_table/dashboard/negative_review.lvdash.json # Path to your dashboard in the workspace
      target_path: /Workspace/Shared/DeployedDashboards/MyDashboard # Desired path in the target wo
      warehouse_id: b4b78d448da57e18
      display_name: negative_review_dashboard
  jobs:
    daily_refresh_generator:
      name: daily_refresh_generator
      tasks:
        - task_key: run_negative_gen
          notebook_task:
            notebook_path: /Workspace/src/negative_review_generator
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZE


      # can send an email on pipeline failure, although GitHub will also do this
      # email_notifications:
      #   on_failure:
      #     - user@email.com

