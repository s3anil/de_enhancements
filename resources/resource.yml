resources:
  jobs:
    daily_refresh_generator:
      name: daily_refresh_generator
      tasks:
        - task_key: run_negative_gen
          notebook_task:
            notebook_path: Workspace/Users/dde9580d-c31d-4acf-8481-2cacab11e873/.bundle/de_enhancements/dev/files/negative_review_generator.py
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZE


      # can send an email on pipeline failure, although GitHub will also do this
      # email_notifications:
      #   on_failure:
      #     - user@email.com

