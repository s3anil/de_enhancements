resources:
  dashboards:
    negative_review:
      warehouse_id: 
      file_path: ../../negative_review.lvdash.json
      display_name: negative_review_dashboard

  jobs:
    daily_refresh_generator:
      name: daily_refresh_generator
      tasks:
        - task_key: run_negative_gen
          notebook_task:
            notebook_path: /Workspace/src/negative_review_generator
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZE
      tasks:
        - task_key: dashboard_task
          depends_on:
            - task_key: run_negative_gen
          dashboard_task:
             subscription:
              subscribers:
              - user_name: s3anil@gmail.com # Replace this with the email of snapshot subscriber email
              paused: false
              warehouse_id: b4b78d448da57e18
              dashboard_id: 01f09471c5ca1697990ecf53a0f9a97d


      # can send an email on pipeline failure, although GitHub will also do this
      # email_notifications:
      #   on_failure:
      #     - user@email.com

